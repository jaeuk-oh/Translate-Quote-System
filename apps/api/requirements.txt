# ── 웹 프레임워크 ─────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0

# ── 데이터 검증 ───────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1

# ── 데이터베이스 ──────────────────────────
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# ── 인증 ─────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# ── 비동기 작업 큐 ────────────────────────
celery[redis]==5.3.6
redis==5.0.4

# ── 파일 스토리지 (S3) ────────────────────
boto3==1.34.98

# ── 이메일 발송 ───────────────────────────
resend==2.0.0

# ── HTTP 클라이언트 ───────────────────────
httpx==0.27.0

# ── Rate Limiting ─────────────────────────
# slowapi: FastAPI용 Rate Limiter. Starlette 미들웨어로 Redis 백엔드 지원.
# limits 라이브러리 기반으로 sliding window / fixed window 알고리즘 내장.
slowapi==0.1.9

# ── SSE (Server-Sent Events) ──────────────
# sse-starlette: FastAPI/Starlette용 SSE 응답 타입.
# EventSourceResponse로 Redis pub/sub 이벤트를 클라이언트에 push.
sse-starlette==2.1.3

# ── 유틸리티 ─────────────────────────────
python-dotenv==1.0.1

# ── 테스트 ───────────────────────────────
# pytest-asyncio: async def 테스트 함수 지원 (asyncio 이벤트 루프 관리)
pytest==8.2.0
pytest-asyncio==0.23.6
# httpx는 위에 이미 선언됨 — ASGITransport로 FastAPI 테스트 클라이언트 지원
# fakeredis: Redis를 인메모리로 모킹 — 실제 Redis 없이 Rate Limit, 락 테스트
fakeredis==2.23.2
